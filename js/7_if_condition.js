// Question: Take input from user and display if it is position or negative

var num = parseFloat(prompt("Enter a number to check"))

if (num > 0) {
    document.write(num+" is positive number.")
}
else {
    document.write(num+" is negative number.")
}


/*
Syntax:
     if (condition) {
        -- code block --
     }
     else if (condition) {
        -- code block --
     }
     else {
        -- code block --
     }
*/